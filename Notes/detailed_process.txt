Subdomain enumeration

Input: wildcard_domains.txt
Process: Create folders for each new subdomain discovered inside the *.xxx.com fodler
Output format: individual subdomains
Asset type: discovered_domains
Output: discovered_hosts.txt
May or may not be live subdomains, so can't say for sure this will be correct.


Content Discovery

Input: discovered_hosts.txt
Input - db: discovered_domains
SELECT * FROM asssets where asset_type = discovered_domains
	(sort by unique values)

Output: discovered_content.txt
	Contains all unique discovered content.
Output db

Recursebuster regex:
Get URLs:
[A-Z]{3,5}\s(\S*)

Get content length:
Length:\s(\S[0-9]*)


Authenticated Content Discovery - A useful feature in the future

Content Monitoring:
Run Gobuster -l on all discovered content.
Might be better to use something else, as gobuster